<div class="container">
 <div class ="row" >
   <div class="col-md-9 off-set-3">

        <h1>部屋一覧(全<%= Room.all.count %>件)</h1>
        
         <div class="d-flex flex-wrap">

            <% @rooms.each do |room| %>
      
         


            <div class="card-body text-center">
              <%= link_to room.name, room_path(room) %>
            </div>
            
           <% end %>
        </div>
        
   </div>
 </div>
</div>