<div class="container">
 <div class ="row" >
   <div class="col-md-9 off-set-3">

        <h1>貸出物一覧(全<%= Lending.all.count %>件)</h1>

         <div class="d-flex flex-wrap">

            <% @lendings.each do |lending| %>




            <div class="card-body text-center">
              <%= link_to lending.name, lending_path(lending) %>
            </div>

           <% end %>
        </div>

   </div>
 </div>
</div>