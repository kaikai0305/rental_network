<div class="nested-fields">
  <div class="field">
      <%= f.fields_for :reservation_lendings do |ff| %>
       <%= ff.text_field :lending_id %>
       <%= ff.number_field :quantity %>
       <%= ff.text_field :memo %>
      <% end %>
  </div>
  <div class="field">
    <%= link_to_remove_association "貸出物を削除", reservation_lendings %>
  </div>
  <hr>
</div>